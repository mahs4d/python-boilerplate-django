from utils.errors import CustomApiError
from utils import errors

def get_server_info():
    raise Exception('sdfsfsdf')
    return {
        'version_code': '1.0.0',
        'status': 'healthy',
    }
